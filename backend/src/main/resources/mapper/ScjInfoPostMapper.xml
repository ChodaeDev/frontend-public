<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chodae.mapper.ScjInfoPostMapper">

    <resultMap id="ScjInfoResponseMap" type="com.chodae.dto.ScjInfoResponse">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="phone" column="phone"/>
        <result property="counselType" column="counsel_type"/>
        <result property="commentCount" column="comment_count"/>
        <result property="visibilityLevel" column="visibility_level"/>
        <result property="createDate" column="create_date"/>
        <result property="modifiedDate" column="modified_date"/>
    </resultMap>

    <select id="findAll" resultMap="ScjInfoResponseMap">
        SELECT id, title, content, user_id, user_name, phone, counsel_type,
               comment_count, visibility_level, create_date, modified_date
        FROM scj_info_post
        ORDER BY create_date DESC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM scj_info_post
    </select>

    <select id="findAllWithPaging" resultMap="ScjInfoResponseMap">
        SELECT id, title, content, user_id, user_name, phone, counsel_type,
               comment_count, visibility_level, create_date, modified_date
        FROM scj_info_post
        <choose>
            <when test="sortColumn == 'title' and sortOrder == 'ASC'">ORDER BY title ASC</when>
            <when test="sortColumn == 'title' and sortOrder == 'DESC'">ORDER BY title DESC</when>
            <when test="sortColumn == 'create_date' and sortOrder == 'ASC'">ORDER BY create_date ASC</when>
            <when test="sortColumn == 'comment_count' and sortOrder == 'ASC'">ORDER BY comment_count ASC</when>
            <when test="sortColumn == 'comment_count' and sortOrder == 'DESC'">ORDER BY comment_count DESC</when>
            <otherwise>ORDER BY create_date DESC</otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findByUserId" resultMap="ScjInfoResponseMap">
        SELECT id, title, content, user_id, user_name, phone, counsel_type,
               comment_count, visibility_level, create_date, modified_date
        FROM scj_info_post
        WHERE user_id = #{userId}
        ORDER BY create_date DESC
    </select>

    <select id="findById" resultMap="ScjInfoResponseMap">
        SELECT id, title, content, user_id, user_name, phone, counsel_type,
               comment_count, visibility_level, create_date, modified_date
        FROM scj_info_post
        WHERE id = #{id}
    </select>

    <select id="findByIdAndUserId" resultMap="ScjInfoResponseMap">
        SELECT id, title, content, user_id, user_name, phone, counsel_type,
               comment_count, visibility_level, create_date, modified_date
        FROM scj_info_post
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO scj_info_post (title, content, user_id, user_name, phone, counsel_type, comment_count, visibility_level, create_date, modified_date)
        VALUES (#{title}, #{content}, #{userId}, #{userName}, #{phone}, #{counselType}, 0, #{visibilityLevel}, NOW(), NOW())
    </insert>

    <update id="updateCommentCount">
        UPDATE scj_info_post SET comment_count = #{commentCount}, modified_date = NOW() WHERE id = #{id}
    </update>

    <update id="updateVisibilityLevel">
        UPDATE scj_info_post SET visibility_level = #{visibilityLevel}, modified_date = NOW() WHERE id = #{id}
    </update>
</mapper>
